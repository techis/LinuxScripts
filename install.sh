#!/bin/bash

#Install script for linux scripts

#Install pic.sh and add the alias. Since there is only one script currently this script only installs it. 

echo "alias scrot='~/pic.sh'" >> ~/.bashrc

source .bashrc
